.panel.panel-primary
	.panel-heading.text-center
		h4 New Courses
	.panel-body
		.row(ng-repeat="course in courses | orderBy: 'published':true | limitTo : 10")
			.col-xs-3 {{course.published | date: 'MMM d, yyyy'}}
			.col-xs-9	{{course.name}}